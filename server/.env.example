# MongoDB connection
MONGO_URI=mongodb+srv://username:password@cluster0.mongodb.net/chat

# JWT for authentication
JWT_SECRET=changeme123

# Server port
PORT=5000

# Email (SMTP) settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=youremail@example.com
SMTP_PASS=your-app-password
EMAIL_FROM=youremail@example.com

# URLs for frontend & backend
FRONTEND_URL=http://localhost:5500/Frontend
BACKEND_HOST=http://localhost:5000

# Optional defaults
GRIDFS_BUCKET=uploads
DEFAULT_AVATAR_URL=https://example.com/default-avatar.png
